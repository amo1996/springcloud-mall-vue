server:
  port: 8507
spring:
  application:
    name: spring-cloud-order
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: provider-order
#  Ϊfeign��ӻ��˷���
feign:
  hystrix:
    enabled: true